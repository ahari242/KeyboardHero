@model IEnumerable<KeyboardHero.Models.Song>


<h1>Choose a song:</h1>
@{ 
    foreach(var song in Model)
    {
        <p>
            <h2>
                @Html.ActionLink(song.artist + " - " + song.title, "Play", "Songs",new { id = song.id },new { })
            </h2>
        </p>
    }

}